version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-dirs:
    - vendor
